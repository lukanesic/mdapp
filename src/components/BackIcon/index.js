import React from 'react'
import { IoIosArrowBack } from 'react-icons/io'

const BackIcon = ({ onClick }) => {
  return <IoIosArrowBack onClick={onClick} className='backicon' />
}

export default BackIcon
